pre存的是每个输出条目的前缀，就是用来 描述分支 的字符串。

先列出一个目录里面的各个项目。
遍历一遍分成文件夹和文件。

遍历文件名。
	输出 前缀加上文件名。

遍历文件夹。
	如果是 最后一个条目， 要修改一下前缀。把 ‘├’ 改成 ‘└’，
	同时要初始化，这个文件夹的下个目录的前缀，nextpre，
		nextpre 的内容，应该对应本层文件夹的前缀中的 '├/└' 是哪一个字符
		对应的选择‘    ’或者‘│   ’ 插入。 
	输出 前缀加上文件夹名。
		tree进入这个文件夹的遍历。


